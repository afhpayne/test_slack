-----------------------------------------------------------------------|
JabRef is an open source bibliography reference manager. The native
file format used by JabRef is BibTeX, the standard LaTeX bibliography
format.

This package is built from the developer's binary .rpm package.  You
do not need to install Java, and there are no additional dependencies.
